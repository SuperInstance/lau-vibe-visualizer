[package]
name = "lau-vibe-visualizer"
version = "0.1.0"
edition = "2021"
description = "Visualizes the mono-dimensional vibe scalar as voxel colors, weather, and world state"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
